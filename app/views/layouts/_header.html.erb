<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse"
        data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t("layouts.header.demo_by_phuoc_tien"), root_path, class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t("layouts.header.home"), root_path %></li>
        <% if user_signed_in? %>
          <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              <%= t "layouts.header.user" %>
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("layouts.header.post"),
                posts_path %></li>
              <li><%= link_to t("layouts.header.profile"),
                user_path(current_user) %></li>
              <li><%= link_to t("layouts.header.setting"),
                edit_user_registration_path %></li>
              <li><%= link_to t("layouts.header.logout"),
                destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
          <li class="dropdown notification-dropdown">
            <%= render "notifications/notifications" %>
          </li>
        <% else %>
          <li><%= link_to t("layouts.header.signup"), new_user_registration_path  %></li>
          <li><%= link_to t("layouts.header.login"), new_user_session_path  %></li>
        <% end %>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <span class="glyphicon glyphicon-search"></span>
          </a>
          <ul class="dropdown-menu">
            <li>
              <div>
                <%= form_tag posts_path, method: :get, class: "form-inline",
                  enforce_utf8: false do %>
                  <%= text_field_tag :query, params[:query], class: "form-control" %>
                <% end %>
              </div>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
